include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Projeto usa muitos construtores sem "const" propositalmente (telas dinâmicas).
    # Deixe as regras padrão do flutter_lints ativas; ajuste aqui se necessário.
